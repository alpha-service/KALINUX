# 🚀 БЫСТРОЕ ОБНОВЛЕНИЕ С ЛОКАЛЬНОГО КОМПЬЮТЕРА

## ⚡ Способ 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

Просто запустите один скрипт:

```powershell
cd C:\Users\KALINUX\Documents\GitHub\KALINUX
.\upload-to-vps.ps1
```

**Скрипт автоматически:**
- ✅ Создаст архив проекта (без node_modules, .git и т.д.)
- ✅ Загрузит на VPS
- ✅ Остановит старую версию
- ✅ Распакует и запустит новую
- ✅ Покажет статус

**Время: ~3-5 минут**

---

## ⚡ Способ 2: Через SCP вручную

```powershell
# 1. Создайте архив
cd C:\Users\KALINUX\Documents\GitHub\KALINUX
Compress-Archive -Path * -DestinationPath pos.zip -Force -Exclude node_modules,.git,build

# 2. Загрузите на VPS
scp pos.zip root@72.61.111.39:/tmp/

# 3. На VPS выполните:
ssh root@72.61.111.39
cd /tmp
unzip -o pos.zip -d /opt/pos-new
cd /opt/pos && docker-compose down
rm -rf /opt/pos && mv /opt/pos-new /opt/pos
cd /opt/pos && docker-compose up -d --build
```

---

## ⚡ Способ 3: Через Git (если код на GitHub актуальный)

```bash
# На VPS
ssh root@72.61.111.39
cd /opt/pos
docker-compose down
git pull origin main
docker-compose up -d --build
docker-compose logs -f
```

---

## 🔧 Что исправлено

✅ **Dockerfiles обновлены** - добавлен `--legacy-peer-deps` для решения проблем с зависимостями  
✅ **Код на GitHub актуальный** - последний push включает все исправления  
✅ **Готов скрипт автозагрузки** - `upload-to-vps.ps1` для автоматического развертывания

---

## 📋 После обновления

Проверьте что всё работает:

```bash
ssh root@72.61.111.39
cd /opt/pos
docker-compose ps          # Все 3 контейнера должны быть Up
docker-compose logs -f     # Проверьте логи
```

Откройте в браузере:
- http://72.61.111.39:8080
- http://pos.kruhn.eu:8080

---

## 🎯 ИСПОЛЬЗУЙТЕ upload-to-vps.ps1

Это самый простой способ! Просто запустите:

```powershell
.\upload-to-vps.ps1
```

Готово! 🎉
