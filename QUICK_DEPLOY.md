# ⚡ Быстрый старт - Развертывание POS на VPS

## 🎯 Цель
Развернуть POS систему на **pos.kruhn.eu:8080**

## 📋 Что нужно сделать на VPS

### 1️⃣ Подключитесь к VPS
```bash
ssh root@YOUR_VPS_IP
```

### 2️⃣ Клонируйте проект
```bash
# Установите git
apt update && apt install git -y

# Клонируйте репозиторий
cd /opt
git clone https://github.com/YOUR_USERNAME/KALINUX.git pos
cd pos
```

### 3️⃣ Запустите автоматическую установку
```bash
chmod +x deploy-docker.sh
./deploy-docker.sh
```

Скрипт автоматически:
- ✅ Установит Docker и Docker Compose
- ✅ Соберет и запустит все контейнеры (MongoDB, Backend, Frontend)
- ✅ Настроит порт 8080
- ✅ Опционально установит Nginx и SSL

### 4️⃣ Проверьте работу
```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f

# Открыть в браузере
http://pos.kruhn.eu:8080
```

---

## 🔧 Ручная установка (если нужно)

```bash
# 1. Установите Docker
curl -fsSL https://get.docker.com | sh
apt install docker-compose -y

# 2. Перейдите в папку проекта
cd /opt/pos

# 3. Запустите
docker-compose up -d --build

# 4. Проверьте
docker-compose ps
```

---

## 📊 Управление

```bash
# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Обновление кода
cd /opt/pos
git pull
docker-compose up -d --build

# Логи
docker-compose logs -f frontend
docker-compose logs -f backend
docker-compose logs -f mongodb
```

---

## 🌐 Настройка DNS

**Важно!** Перед развертыванием убедитесь что домен настроен:

1. Зайдите в панель управления DNS (где куплен домен kruhn.eu)
2. Добавьте A-запись:
   - **Тип:** A
   - **Имя:** pos
   - **Значение:** IP_адрес_вашего_VPS
   - **TTL:** 300 (или по умолчанию)

3. Проверьте DNS (подождите 5-10 минут):
```bash
nslookup pos.kruhn.eu
# или
ping pos.kruhn.eu
```

---

## 🔒 SSL сертификат (HTTPS)

После того как сайт доступен по HTTP, установите SSL:

```bash
# Установите Certbot
apt install certbot python3-certbot-nginx -y

# Получите сертификат
certbot certonly --standalone -d pos.kruhn.eu

# Или используйте скрипт deploy-docker.sh (он предложит установить SSL)
```

---

## ❓ Решение проблем

### Порт 8080 занят
```bash
netstat -tulpn | grep 8080
fuser -k 8080/tcp
```

### Контейнеры не запускаются
```bash
docker-compose logs
docker-compose down
docker-compose up -d --build
```

### База данных не подключается
```bash
docker exec -it pos_mongodb mongosh -u admin -p changeme
```

---

## 📱 Контакты

После развертывания приложение доступно:
- **Прямой доступ:** http://YOUR_VPS_IP:8080
- **Через домен:** http://pos.kruhn.eu:8080
- **С SSL:** https://pos.kruhn.eu (после настройки SSL и Nginx)

---

## 📦 Что внутри

- **Frontend:** React приложение (порт 8080)
- **Backend:** Node.js API (порт 8000, внутри Docker)
- **Database:** MongoDB 7 (порт 27017, внутри Docker)
- **Nginx:** Reverse proxy (опционально)

Все настроено и готово к работе! 🚀
