# 🔄 Обновление проекта на VPS

## Вариант 1: С локального компьютера (рекомендуется)

### Способ А: Через SCP (быстро)

**С вашего Windows компьютера:**

```powershell
# Перейдите в папку проекта
cd C:\Users\KALINUX\Documents\GitHub\KALINUX

# Создайте архив (исключая ненужные файлы)
$exclude = @('node_modules', '.git', 'build', 'uploads', '*.log')
Compress-Archive -Path * -DestinationPath pos-deploy.zip -Force

# Загрузите на VPS
scp pos-deploy.zip root@72.61.111.39:/tmp/
```

**На VPS:**

```bash
# Распакуйте и установите
cd /tmp
unzip -o pos-deploy.zip -d /opt/pos-new
cd /opt/pos-new

# Остановите старую версию
cd /opt/pos && docker-compose down

# Переместите новую версию
rm -rf /opt/pos
mv /opt/pos-new /opt/pos
cd /opt/pos

# Запустите
docker-compose up -d --build

# Проверьте
docker-compose logs -f
```

---

### Способ Б: Через rsync (синхронизация)

**Установите rsync на Windows:**
```powershell
# В PowerShell
winget install --id=Rsync.Rsync  -e
```

**Синхронизируйте проект:**
```powershell
cd C:\Users\KALINUX\Documents\GitHub\KALINUX

rsync -avz --exclude 'node_modules' --exclude '.git' --exclude 'build' --exclude 'uploads' --exclude '*.log' ./ root@72.61.111.39:/opt/pos/
```

**На VPS:**
```bash
cd /opt/pos
docker-compose up -d --build
```

---

## Вариант 2: Через Git (обновить GitHub)

### Шаг 1: Запушьте актуальный код на GitHub

```powershell
cd C:\Users\KALINUX\Documents\GitHub\KALINUX

# Проверьте статус
git status

# Добавьте все изменения
git add .

# Коммит
git commit -m "Update to latest version with fixes"

# Пуш
git push origin master:main -f
```

### Шаг 2: Обновите на VPS

```bash
cd /opt/pos
docker-compose down
git pull
docker-compose up -d --build
```

---

## Вариант 3: Через WinSCP (графический интерфейс)

1. **Скачайте WinSCP:** https://winscp.net/
2. **Подключитесь к VPS:**
   - Host: `72.61.111.39`
   - User: `root`
   - Password: ваш пароль
3. **Загрузите файлы:**
   - Локальная папка: `C:\Users\KALINUX\Documents\GitHub\KALINUX`
   - Удаленная папка: `/opt/pos`
   - Исключите: `node_modules`, `.git`, `build`
4. **На VPS выполните:**
   ```bash
   cd /opt/pos
   docker-compose up -d --build
   ```

---

## 🔧 Быстрое обновление с исправлениями

Я уже исправил Dockerfiles (добавил `--legacy-peer-deps`). Теперь на VPS выполните:

```bash
# Подключитесь к VPS
ssh root@72.61.111.39

# Обновите код с GitHub
cd /opt/pos
docker-compose down
git pull origin main
docker-compose up -d --build

# Проверьте логи
docker-compose logs -f
```

---

## 📋 Проверка после обновления

```bash
# Статус контейнеров
docker-compose ps

# Все 3 контейнера должны быть Up:
# - pos_mongodb      Up
# - pos_backend      Up  
# - pos_frontend     Up  0.0.0.0:8080->80/tcp

# Логи
docker-compose logs -f frontend
docker-compose logs -f backend

# Проверить сайт
curl http://localhost:8080
```

---

## ⚡ Самый быстрый способ (одна команда)

**Загрузите архив и разверните:**

```bash
# На VPS выполните:
ssh root@72.61.111.39 << 'EOF'
cd /opt/pos
docker-compose down
git pull origin main
docker-compose up -d --build
docker-compose ps
EOF
```

---

## 🐛 Если остались проблемы с зависимостями

Измените локально `package.json` версию react-day-picker:

```json
"react-day-picker": "^9.0.0"
```

Или добавьте в `package.json`:

```json
"overrides": {
  "react-day-picker": {
    "react": "$react"
  }
}
```

Затем:
```bash
npm install --legacy-peer-deps
git add package.json package-lock.json
git commit -m "Fix react-day-picker compatibility"
git push
```

---

## 📞 Итого

**Рекомендуемый порядок действий:**

1. ✅ **Dockerfiles уже исправлены** (я добавил --legacy-peer-deps)
2. ✅ **Исправления на GitHub** 
3. 🔄 **Теперь обновите на VPS:**

```bash
ssh root@72.61.111.39
cd /opt/pos
docker-compose down
git pull
docker-compose up -d --build
docker-compose logs -f
```

Готово! 🚀
