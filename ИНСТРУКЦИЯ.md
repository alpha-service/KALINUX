# 🚀 ИНСТРУКЦИЯ ПО РАЗВЕРТЫВАНИЮ POS НА VPS

## 📋 Что нужно сделать

### 1️⃣ Подключитесь к вашему VPS
\`\`\`bash
ssh root@ВАШ_IP_АДРЕС
# Например: ssh root@185.123.456.789
\`\`\`

### 2️⃣ Запустите одну команду для автоустановки
\`\`\`bash
curl -sSL https://raw.githubusercontent.com/alpha-service/KALINUX/main/install-vps.sh | bash
\`\`\`

### 3️⃣ Готово! ✅
Ваш POS будет доступен по адресу: **http://pos.kruhn.eu:8080**

---

## 📝 Что делает скрипт автоматически:

1. ✅ Обновляет систему
2. ✅ Устанавливает Docker и Docker Compose
3. ✅ Клонирует проект с GitHub
4. ✅ Запускает все контейнеры (MongoDB, Backend, Frontend)
5. ✅ Настраивает порт 8080

**Время установки:** ~5-10 минут

---

## 🌐 ВАЖНО: Настройка домена

**До развертывания** настройте DNS:

1. Зайдите в панель управления вашего регистратора домена (где куплен kruhn.eu)
2. Добавьте A-запись:
   - **Тип:** A
   - **Имя хоста:** pos
   - **IP адрес:** IP_вашего_VPS
   - **TTL:** 300 (или оставьте по умолчанию)

3. Подождите 5-10 минут и проверьте:
\`\`\`bash
ping pos.kruhn.eu
\`\`\`

---

## 🔧 Полезные команды после установки

\`\`\`bash
# Перейти в папку проекта
cd /opt/pos

# Посмотреть статус контейнеров
docker-compose ps

# Посмотреть логи
docker-compose logs -f

# Перезапустить
docker-compose restart

# Остановить
docker-compose down

# Обновить код и перезапустить
git pull && docker-compose up -d --build
\`\`\`

---

## 🔒 Установка HTTPS (SSL) - Опционально

После того как сайт работает по HTTP, можно добавить HTTPS:

\`\`\`bash
# 1. Установите Nginx
apt install nginx -y

# 2. Настройте Nginx как reverse proxy
cat > /etc/nginx/sites-available/pos << 'EOF'
server {
    listen 80;
    server_name pos.kruhn.eu;
    
    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
    }
}
EOF

ln -sf /etc/nginx/sites-available/pos /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx

# 3. Установите SSL сертификат
apt install certbot python3-certbot-nginx -y
certbot --nginx -d pos.kruhn.eu
\`\`\`

После этого сайт будет доступен: **https://pos.kruhn.eu** 🎉

---

## ❓ Решение проблем

### Порт 8080 занят
\`\`\`bash
fuser -k 8080/tcp
cd /opt/pos && docker-compose restart
\`\`\`

### Контейнеры не запускаются
\`\`\`bash
cd /opt/pos
docker-compose logs
docker-compose down
docker-compose up -d --build
\`\`\`

### Не могу подключиться к сайту
1. Проверьте что контейнеры запущены: `docker-compose ps`
2. Проверьте что порт 8080 открыт: `netstat -tulpn | grep 8080`
3. Проверьте DNS: `ping pos.kruhn.eu`
4. Проверьте firewall: `ufw status`

### Нужно открыть порты в firewall
\`\`\`bash
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw allow 8080/tcp  # POS приложение
ufw enable
\`\`\`

---

## 📊 Что установлено

После развертывания у вас будет:

- ✅ **Frontend** (React) - работает на порту 8080
- ✅ **Backend** (Node.js Express) - работает внутри Docker на порту 8000
- ✅ **База данных** (MongoDB 7) - работает внутри Docker
- ✅ **Все контейнеры** автоматически перезапускаются при сбое

---

## 🎯 Дополнительная документация

Если нужно больше информации:
- `START_HERE.md` - основная инструкция (English)
- `QUICK_DEPLOY.md` - краткое руководство
- `DEPLOY_INSTRUCTIONS.md` - полная документация

---

## 📞 Если что-то не работает

1. Посмотрите логи: `cd /opt/pos && docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Проверьте что Docker работает: `docker ps`
4. Перезапустите: `docker-compose restart`

---

## ✨ Готово!

После успешного развертывания:
- 🌐 Откройте http://pos.kruhn.eu:8080
- 🎉 Ваш POS готов к работе!

Удачи! 🚀
